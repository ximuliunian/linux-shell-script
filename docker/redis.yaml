image: redis:latest
container_name: redis
ports:
  - "${PORT_6379}:6379"
volumes:
  # 数据持久化目录
  - "${MOUNT_DIR}/data:/data"
  # 配置文件
  - "${MOUNT_DIR}/redis.conf:/usr/local/etc/redis/redis.conf"
environment:
  - TZ=Asia/Shanghai
